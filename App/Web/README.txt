How to run the web application (Tested on Windows Command Prompt) :

1. In command line, run "npm install" to install dependencies.
2. In command line under this folder, run "npm run web" to launch the browser with the web application.